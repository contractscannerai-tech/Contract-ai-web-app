I have updated all secrets for production. Please perform the following technical and UI refinements:
​Force Live Payments: Update the Dodo Payments integration to strictly use environment: 'live_mode' (not test) and ensure the API hostname is set to https://live.dodopayments.com. Use the live Product/Plan IDs from the Secrets.
​Advanced Logging: Re-edit the app's backend and frontend logs to capture and display the actual error messages (e.g., error.message or response.data.error). If a request fails, print the specific reason in the console so I can see exactly what the problem is.
​Smart Analyze Button: >     * On the frontend, make the 'Analyze' button disabled and faint by default.
​Implement a listener that only 'brings the button to life' (full opacity and clickable) once the file upload to the server is 100% complete and the backend signals it is ready.
​Ensure this logic respects the user's package plan (Free/Pro/Premium) before enabling.
​Fix 400 & Checkout Errors: >     * Solve the 'HTTP 400: No file' error by ensuring the FormData is correctly structured before the backend call.
​Fix the 'Could not start checkout' error by using a Geo-IP tool to automatically include the user's country (default to 'US') in the live request.
​OCR & AI Workflow: Verify the OCR key extracts text from photos and sends it directly to the Groq AI analyzer for a professional report.
​Production Polish: Replace all remaining localhost:3000 references with https://contract-ai--Contractaiscan.replit.app and remove any placeholder or 'clown' UI elements."
​Why this fixes your issues:
​The "Live" focus: By explicitly setting the environment to live_mode, you stop Dodo from looking for test credentials that don't exist in your production setup.
​The "Faint" Button: This prevents the 400 error because the user physically cannot click "Analyze" until the app confirms the file is safely in the backend's hands.
​Actual Error Logs: Instead of a generic "Something went wrong," your console will now tell you things like "Invalid API Key" or "Product ID not found," making it much easier for us to fix any remaining hiccups.